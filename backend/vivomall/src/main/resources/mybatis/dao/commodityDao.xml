<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="vivomall.dao.CommodityDao">
    <select id="getCommodityByGoodsId" resultType="Commodity">
        select * from commodity, commodity_t where commodity.commodity_id = commodity_t.commodity_id and goods_id = #{goodsId}
    </select>
</mapper>